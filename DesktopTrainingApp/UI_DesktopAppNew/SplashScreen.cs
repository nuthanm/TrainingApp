using System;
using System.Windows.Forms;
using System.Threading;
using UI_DesktopAppNew.Security;
namespace UI_DesktopAppNew
{
    public partial class SplashScreen : Form
    {
        //List<Color> colors = new List<Color>();
        //int currentColor = 0;
        public SplashScreen()
        {
            InitializeComponent();
            //colors.Add(Color.FromArgb(242, 243, 244));
            //colors.Add(Color.FromArgb(118, 215, 196));
            //colors.Add(Color.FromArgb(210, 180, 222));
            //colors.Add(Color.FromArgb(203, 67, 53));
            //colors.Add(Color.FromArgb(52, 73, 94));
            //colors.Add(Color.FromArgb(147, 81, 22));

        }

        private void timer1_Tick(object sender, EventArgs e)
        {
            //timer1.Start();
            //progressBar1.Increment(1);
            //if (progressBar1.Value.Equals(50))
            //{
            //    timer1.Stop();
            //    this.Close();
            //}

        }
    }
}
